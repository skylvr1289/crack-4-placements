#include <stdlib.h>
#define N 100
#include <stdlib.h>
#define sizeof_type( type )  (size_t)((type*)1000 + 1 )-(size_t)((type*)1000)
void main()
{
int i, n = 20;
size_t size = (size_t)(1 + ((int*)0));
printf("%d",size);
}
